 ┌───────────────────────────┐
 │         Prompts           │
 │   (Input text/questions)  │
 └────────────┬──────────────┘
              │
              ▼
 ┌───────────────────────────┐
 │     ModelManager          │
 │ - Load / unload models    │
 │ - Manage GPU/CPU memory   │
 └────────────┬──────────────┘
              │
              ▼
 ┌───────────────────────────┐
 │    InferenceEngine        │
 │ - Generates text from     │
 │   prompts using models    │
 │ - Measures latency,       │
 │   throughput, memory      │
 └────────────┬──────────────┘
              │
      ┌───────┴────────┐
      │                │
      ▼                ▼
┌───────────────┐   ┌─────────────────┐
│ BenchmarkSuite│   │ QualityMetrics  │
│ - Runs model  │   │ - Evaluates     │
│   multiple    │   │   generated text│
│   prompts     │   │ - BLEU, ROUGE,  │
│ - Aggregates  │   │   similarity,   │
│   latency,    │   │   length ratio  │
│   throughput  │   └─────────────────┘
└───────────────┘
      │
      ▼
┌───────────────────────────┐
│ Benchmark Results &       │
│ Quality Scores            │
│ - Speed, memory metrics   │
│ - Text quality metrics    │
│ - Comparisons between     │
│   models / quantizations  │
└───────────────────────────┘